unzip nome-do-arquivo.zip -d nome-da-pastacd /sdcard/Downloadpkg update && pkg upgrade
pkg install gitcd /caminho/para/sua/pastagit inithttps://github.com/SEU_USUARIO/SEU_REPOSITORIO.gitgit add .git commit -m "Primeiro commit"git branch -M main
git push -u origin maingit init Vem-que-tem
Site de vendas confiável 
